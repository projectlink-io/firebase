import admin from 'firebase-admin'
import functions from 'firebase-functions-test'
functions()

describe('Organizations', () => {
  test('it should assign a geohash', async () => {
    expect(true).toBe(true)
  })
})