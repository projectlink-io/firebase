import React from 'react';
import logo from './logo.svg';
import { Flex, Input, FormControl } from 'chakra-ui';

const App: React.FC = () => {
  return (
    <Flex>
      <FormControl>
        <Input />
      </FormControl>
    </Flex>
  );
}

export default App;
